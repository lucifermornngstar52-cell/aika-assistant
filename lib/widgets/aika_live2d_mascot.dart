import 'package:flutter/material.dart';
/// Stub — Live2D removed. Use AikaAvatar widget instead.
enum AikaLive2DState { idle, listening, thinking, talking, greeting }

class AikaLive2DMascot extends StatelessWidget {
  const AikaLive2DMascot({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
